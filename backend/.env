# Archivo de configuración de variables de entorno para el proyecto.
# Estas variables son sensibles y no deben ser compartidas públicamente ni subidas a control de versiones (ej. Git).

# URI de conexión a la base de datos MongoDB.
# Formato: mongodb://[usuario:contraseña@]host:puerto/nombreBaseDeDatos
MONGO_URI=mongodb://localhost:27017/taskmanager

# Puerto en el que se ejecutará el servidor backend.
PORT=3000

# Secreto JWT (JSON Web Token) utilizado para firmar y verificar tokens de autenticación.
JWT_SECRET=miclavesecreta123